#pragma once

// standard input and output library for printing to console
#include <iostream>
// used for formatting output like setting field width
#include <iomanip>

// smart pointers live here
// we use unique_ptr to manage memory automatically without manual delete
#include <memory>
// dynamic array container that grows automatically
#include <vector>

// standard exception classes for error handling
#include <exception>
#include <stdexcept>

// string class implementation
#include <string>
// c-style string functions
#include <cstring>

// debugging tool to check assumptions
#include <cassert>
// c++20 feature that lets us constrain template types
#include <concepts>

// contains pair and other utility functions
#include <utility>
// standard algorithms like sort and copy
#include <algorithm>

// string streams for parsing text
#include <sstream>
// file streams for reading and writing files
#include <fstream>

// doubly linked list container
#include <list>
// queue container adapter
#include <queue>

// stack container adapter
#include <stack>
// hash map implementation for o(1) lookups
#include <unordered_map>

// represents a value that might be missing
#include <optional>
// represents a value that can be one of several types
#include <variant>

// ordered map implementation (red-black tree)
#include <map>
// used to get max values for types
#include <limits>

// support for function objects and lambdas
#include <functional>
// stack container adapter
#include <stack>
// hash map implementation for o(1) lookups
#include <unordered_map>

// represents a value that might be missing
#include <optional>
// represents a value that can be one of several types
#include <variant>

// ordered map implementation
#include <map>
// used to get max values for types
#include <limits>

// support for function objects and lambdas
#include <functional>